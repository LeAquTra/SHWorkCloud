> 📄 本文档是 `docs/` 目录同名文件的**镜像副本**（供前端同学在 homework-web 里直接翻阅）。
> 后端改动后请以 `docs/` 下的版本为准，**不要只改这个副本**。

# 作业云盘 前端对接指南

> 面向**前端开发**。后端只提供 REST 接口，本文档给出页面怎么接、每个流程怎么实现、以及会踩的坑。
> 逐条接口的完整参数与响应示例见 [`后端接口手册.md`](后端接口手册.md)；设计背景见 [`SHWordCloud_Standard_v2.0.md`](SHWordCloud_Standard_v2.0.md)。

---

## 1. 五分钟接入

### 1.1 基本信息

| 项 | 值 |
|----|-----|
| 后端地址 | 开发：`http://localhost:8081/api`（或由 Vite proxy 转发 `/api`）；生产：Nginx 同源反代 `/api` |
| 鉴权 | 请求头 `Authorization: <token>`，**不加 `Bearer` 前缀** |
| 返回体 | `{ code, message, data }`；`code === 0` 为成功 |
| HTTP 状态 | 业务错误一律 **200**；只有未登录 → **401**、无权限 → **403** |
| 分页 | `page` 从 **1** 开始；返回 `{ page, size, total, records }` |

### 1.2 建议的请求封装

```ts
// api/http.ts
import axios from 'axios'

const TOKEN_KEY = 'sc_token'
// 机房是共用电脑：用 sessionStorage，关掉标签页即失效。
// 用 localStorage 会让下一位同学打开浏览器直接进上一位的网盘。
export const getToken = () => sessionStorage.getItem(TOKEN_KEY) || ''
export const setToken = (t: string) => sessionStorage.setItem(TOKEN_KEY, t)
export const clearToken = () => sessionStorage.removeItem(TOKEN_KEY)

export class ApiError extends Error {
  constructor(public code: number, message: string) { super(message) }
}

const instance = axios.create({ baseURL: '/api', timeout: 60000 })

instance.interceptors.request.use((cfg) => {
  const token = getToken()
  if (token) cfg.headers.set('Authorization', token)
  return cfg
})

function toLogin(hint: string) {
  clearToken(); sessionStorage.clear()
  if (!location.pathname.startsWith('/login')) location.assign('/login')
  console.warn(hint)
}

async function request<T>(cfg): Promise<T> {
  const { data: body } = await instance.request<{ code: number; message: string; data: T }>(cfg)
  switch (body.code) {
    case 0: return body.data
    case 40100: toLogin('未登录或会话已失效'); throw new ApiError(body.code, body.message)
    case 40117: toLogin('账号已被禁用'); throw new ApiError(body.code, '账号已被禁用，请联系老师')
    case 40119:                                  // 首登强制改密
      if (!location.pathname.startsWith('/change-password')) location.assign('/change-password')
      throw new ApiError(body.code, body.message)
    default: throw new ApiError(body.code, body.message || '请求失败')
  }
}

export const get = <T>(url: string, params?: object) => request<T>({ url, method: 'GET', params })
export const post = <T>(url: string, data?: unknown, cfg = {}) => request<T>({ url, method: 'POST', data, ...cfg })
export const put = <T>(url: string, data?: unknown) => request<T>({ url, method: 'PUT', data })
export const del = <T>(url: string, data?: unknown, params?: object) =>
  request<T>({ url, method: 'DELETE', data, params })
```

### 1.3 会话与空闲登出

- 登录成功后 `setToken(data.token)`，并把 `mustChangePassword` 存下来；
- **路由守卫**：无 token → `/login`；`mustChangePassword` → 强制留在 `/change-password`；
- **空闲自动登出**：后端 30 分钟无操作失效（`sa-token.active-timeout`），前端建议在 **20 分钟**时弹"即将退出"提醒，
  60 秒后调 `POST /auth/logout` 并跳登录页。这能避免学生填了半天表单突然 401；
- **离开页面拦截**：上传队列里有未完成任务时 `beforeunload` 弹确认 —— 学生下课前最容易漏掉这一步。

---

## 2. 核心概念：viewType（决定了怎么渲染）

文件列表里每个文件都带 `viewType`，前端**按它分流即可，不要自己写后缀白名单**：

| viewType | 渲染 | 接口 |
|----------|------|------|
| `image` | `<img>` / `el-image` | `preview-url`（签名）或 `GET /files/{id}/preview` |
| `pdf` | ⚠️ **不预览**（需求方要求）：点击整行不弹窗、下拉框里没有"在线预览" | 只能 `download-url` |
| `video` | `<video>` | 同上（支持 Range，可拖进度条） |
| `audio` | `<audio>` | 同上（支持 Range） |
| `text` | 代码/文本查看器 | `GET /files/{id}/text` |
| `office` | `docx`/`xlsx` 用返回的 `html` 渲染结构化版面，其它回退纯文本 | `GET /files/{id}/text` |
| `none` | 不预览，只给"下载" | `download-url` |

> ⚠️ **判断"能不能预览"请用 `previewable` 字段，不要用 `viewType !== 'none'`**：
> `pdf` 的 `viewType` 仍然是 `pdf`（图标与分类还要用），但 `previewable=false`，
> 而且 `GET /files/{id}/preview-url` 会返回 `40073`。用 viewType 判断的话，
> 界面会出现一个点了必报错的"在线预览"。

```ts
function renderKind(item: FileItemVO) {
  if (!item.previewable) return 'downloadOnly'   // ← 以服务端为准
  switch (item.viewType) {
    case 'image': return 'img'
    case 'video': return 'video'
    case 'audio': return 'audio'
    case 'text':
    case 'office': return 'text'
    default: return 'downloadOnly'
  }
}
```

---

## 3. 八个关键流程

### 3.1 登录 / 首次改密

```ts
// 登录页
const cfg = await get<RegisterConfigVO>('/auth/register-config')   // 公开接口，先问清楚流程
showRegisterEntry.value = cfg.registerEnabled                      // 不开放就别显示注册入口

// 人机验证：先问服务端，不要自己猜（题库为空时服务端会自动返回 false）
const human = await get<HumanCheckVO>('/auth/human-check')
const passToken = human.login ? await askUserToPassCaptcha() : undefined

const login = await post<LoginVO>('/auth/login', { login: 学号, password: 密码, captchaPassToken: passToken })
setToken(login.token)
router.replace(login.mustChangePassword ? '/change-password' : '/')
```

⚠️ **收到 `40105` / `40103` 时不要直接报错**，那是"请先完成人机验证"的意思：
弹一次验证码窗口 → 拿到新凭证 → **重试一次**原来的请求。原因有两个：
① 配置可能在页面停留期间变了（管理员刚上传了第一批题目）；
② 凭证有有效期（默认 5 分钟），页面开久了就过期了。

```ts
// 改密（首登强制走这里）
await post('/auth/password', { oldPassword, newPassword, confirmPassword })
router.replace('/')
```

> 密码规则与后端一致：8~32 位、必须同时含字母和数字、不能有空格、不能与学号相同。
> 前端最好本地先校验一次，别等提交后才报错。

### 3.2 注册（可选通道）

```ts
const cfg = await get<RegisterConfigVO>('/auth/register-config')

// 只有 human.register=true 时才需要这两步
if ((await get<HumanCheckVO>('/auth/human-check')).register) {
  const cap = await get<CaptchaVO>('/auth/captcha')        // ⚠️ GET，不是 POST
  // { captchaId, type, imageUrl, width, height, prompts }
  // type: 1 字符输入 / 2 单选 / 3 点选（clicks 要换算回原图像素）
  const { captchaPassToken } = await post<{ captchaPassToken: string }>('/auth/captcha/verify', {
    captchaId: cap.captchaId, answer, clicks,
  })
  passToken = captchaPassToken
}

await post('/auth/email-code', { email, captchaPassToken: passToken })   // 发邮件验证码
await post('/auth/register', { email, emailCode, password })             // 注册
```

⚠️ 注册提交**不需要**带 `captchaPassToken`（它已在发邮件码时被一次性消费）。
也正因为是**一次性**的：点"重新发送"要重新过一次验证码，不能复用上一个凭证。

### 3.3 上传（最复杂的一块）

```ts
// ① 先查容量，空间不够就别开始传
const quota = await get<QuotaVO>('/user/quota')
if (file.size > quota.free) {
  // 提示并给"清空回收站"入口：回收站占用在 quota.recycleUsed
  return
}

// ② 算文件指纹（大文件用抽样指纹，避免为秒传先算几十秒哈希）
//    ⚠️ 不要用 crypto.subtle / crypto.randomUUID：内网 http 不是安全上下文，这些 API 不存在
const fingerprint = await computeFingerprintInWorker(file)

// ③ 试秒传；hit=false 表示未命中，继续正常上传
const instant = await post<CommitVO>('/files/instant-upload', {
  md5: fingerprint, parentId, name: file.name, size: file.size, contentType: file.type,
})
if (instant.hit) { refreshList(); return }

// ④ 申请上传凭证（ObjectKey 由服务端签发，前端不要自己拼 key）
//    题库启用后这一步会要人机验证：建议在**开始上传前**就把验证码走完
//    （本项目在"选完文件、开始建目录"时弹窗），否则用户会先盯几秒指纹进度条
//    再被要求做验证码，体验很割裂。
//    通过一次后有 10 分钟免验证窗口（app.captcha.upload-pass-minutes），
//    整文件夹几十个文件只打扰一次。
const ticket = await post<UploadTicketVO>('/oss/ticket', {
  name: file.name, size: file.size, contentType: file.type,
  captchaPassToken: passToken,      // 不需要时传 undefined
})
// 仍返回 40105 -> 弹窗拿新凭证后**重试这一次 ticket**，不要整批作废

if (ticket.partCount <= 1) {
  // ⑤a 小文件：单次 PUT
  const put = await post<PutUrlVO>('/oss/put-url', {
    uploadToken: ticket.uploadToken, contentType: file.type,
  })
  // ⚠️ Content-Type 必须用响应里返回的 contentType，否则 OSS 报 SignatureDoesNotMatch
  await xhrPut(put.url, file, put.contentType, (percent) => progress.value = percent)
} else {
  // ⑤b 大文件：分片
  const { uploadId } = await post<{ uploadId: string }>('/oss/multipart/init', {
    uploadToken: ticket.uploadToken, contentType: file.type,
  })
  const partNumbers = Array.from({ length: ticket.partCount }, (_, i) => i + 1)
  const { contentType, urls } = await post<PartUrlsVO>('/oss/multipart/part-urls', {
    uploadToken: ticket.uploadToken, uploadId, partNumbers,   // 单次最多 500 个
  })
  const etags = await uploadPartsWithConcurrency(file, ticket.partSize, urls, contentType, 2)
  await post('/oss/multipart/complete', { uploadToken: ticket.uploadToken, uploadId, parts: etags })
}

// ⑥ 建立索引（这一步成功才算"保存到网盘"）
const commit = await post<CommitVO>('/files/commit', {
  uploadToken: ticket.uploadToken, parentId, name: file.name, contentType: file.type, md5: fingerprint,
})
// commit.receipt 是提交凭证，界面上要显示出来给学生核对
```

**必须做对的三件事**

1. **`Content-Type` 用响应里返回的那个值**：OSS V1 签名把 `Content-Type` 计入待签字符串，
   自己随便设会导致 `SignatureDoesNotMatch`；
2. **`ETag` 要去掉双引号**再提交给 `complete`（OSS 返回 `"abc"`，提交要 `abc`）；
3. **两阶段进度**：`上传中`（OSS 直传）与`正在登记`（`/files/commit`）必须分开显示。
   OSS 传完 ≠ 保存成功，只显示到 100% 会让学生以为存好了 —— 下课前关机就丢了。

**断点续传**：把 `{ uploadToken, uploadId, partSize, parts }` 存起来（key 里带 userId）。
刷新页面后重新选择同一文件，先调 `GET /oss/multipart/parts?uploadToken=&uploadId=`
拿回服务端已收到的分片，跳过它们继续传 —— 这比 SDK 的内存 checkpoint 可靠。

**暂停 vs 取消**：暂停只 abort 请求、保留断点；取消要调 `POST /oss/multipart/abort`
让服务端清掉未完成分片（否则 OSS 会持续计费）。

### 3.4 在线阅览

```ts
// 图片 / 视频 / 音频（pdf 不在此列，它不可预览）
async function openStreamable(item: FileItemVO) {
  const { url } = await get<{ url: string }>(`/files/${item.id}/preview-url`)   // 10 分钟签名
  if (item.viewType === 'image') return `<img src="${url}">`
  if (item.viewType === 'video') return `<video src="${url}" controls>`
  return `<audio src="${url}" controls>`
}

// 文本 / Office
async function openText(item: FileItemVO) {
  const data = await get<TextContentVO>(`/files/${item.id}/text`)
  // data.charset 会告诉你后端用了哪种编码（UTF-8 / GBK）
  // data.truncated 为 true 时提示"内容过长已截断，请下载查看完整文件"
  // data.hint 有值时要显示（例如"已提取纯文本，排版会丢失"）
  // data.html 有值（docx / xlsx）时可以渲染"原格式"视图：
  //   它是服务端渲染并转义过的结构 HTML，直接 v-html 即可；
  //   本项目仍再做一层白名单过滤（utils/format.ts 的 sanitizeOfficeHtml），双保险。
  return data.html || data.content
}
```

**为什么图片/视频优先用签名地址**：浏览器的 `<img>` / `<video>` **无法携带 `Authorization` 头**。
签名地址是自包含的（有效期写在 URL 里），可以直接放进 `src`。

### 3.5 图片管理（相册）

```ts
const page = await get<PageVO<ImageItemVO>>('/images', { page: 1, size: 60 })
// 每项已经带 previewUrl（1 小时签名），铺网格时不用再为每张图发请求
page.records.forEach(img => {
  // img.previewUrl   → <img src>
  // img.parentId     → "跳转到所在文件夹"
  // img.previewApiUrl → 需要自行带请求头时的流式地址
})
```

### 3.6 头像

```vue
<input type="file" accept="image/jpeg,image/png" @change="onPick" />
```

```ts
async function onPick(e: Event) {
  const file = (e.target as HTMLInputElement).files?.[0]
  if (!file) return
  // 前端先挡一道，给出即时反馈（后端仍会独立校验）
  if (!/^image\/(jpeg|png)$/.test(file.type)) return message.error('只支持 JPG / PNG')
  if (file.size > 5 * 1024 * 1024) return message.error('头像不能超过 5MB')

  const form = new FormData()
  form.append('file', file)
  const profile = await post<UserProfileVO>('/user/avatar', form)   // 返回最新资料
  // profile.avatarUrl 是 1 小时签名地址，可直接给 <img src>
  // profile.avatarVersion 变了就说明换了头像，用它做缓存失效
  // profile.avatarChangeableAt = 下次可修改时间（null 表示现在就能改）
}

// 清除头像（同时删掉 OSS 对象）
await del<UserProfileVO>('/user/avatar')
```

> 后端会校验 magic bytes 并真正解析图片，**只看后缀是不够的** —— 前端也用不着自己解析。

#### 24 小时冷却（必做）

**头像每 24 小时只能改一次**（更换与清除共用），冷却中返回 **40123**。

```ts
const changeableAt = profile.avatarChangeableAt ? new Date(profile.avatarChangeableAt) : null
const locked = !!changeableAt && changeableAt.getTime() > Date.now()
// 不要等用户点了才报错：locked 时把按钮置灰，并**在界面上写清原因与剩余时间**
```

两点经验：

1. **一定要把限制写在界面上**。用户看到按钮灰掉却不知道原因，比看到拒绝更让人恼火；
2. 前端判断不出来时（例如时间格式解析失败）**宁可放开按钮**：真正的拦截在服务端，
   前端不该因为自己解析不了就假装用户不能操作。本项目 `ProfileView.vue` 可直接照搬。

---

### 3.7 公告（按注意力分级打扰用户）

```ts
const items = await get<AnnouncementActiveVO[]>('/announcements/active')
// 任何已登录用户都能拿；只返回"当前生效"的公告（草稿与已撤回对用户不存在）
// 已按「分级降序 → 发布时间降序」排好，前端不要再排
```

**`level` 决定打扰方式，这是这个功能的核心**（别把它只当成一个颜色标签）：

| level | 展示 | 能否关闭 |
|:--:|------|:--:|
| `1` 普通 | 顶部横幅 | ✅ |
| `2` 重要 | 顶部横幅（警示色） | ✅ |
| `3` 紧急 | **强制弹窗**，只有「我已知晓」一个出口 | ❌ |

紧急弹窗必须把 `close-on-click-modal` / `close-on-press-escape` / `show-close` **全部关掉** ——
"紧急"如果不能强制，就只是"比较显眼的横幅"。多条紧急公告排队，点掉一条再弹下一条。

```ts
// 正文是纯文本（后端拒绝 HTML），换行是作者的本意 → white-space: pre-wrap
// 长公告在横幅里只露 2~3 行，完整内容放进顶部的"公告铃铛"里
```

两点实现建议：

1. **已读存在客户端就够**（本项目用 `sessionStorage`）：公告是广播，不是消息队列，
   为它维护 per-user 已读表不值得。用 `sessionStorage` 而不是 `localStorage`，
   是因为机房是**公用电脑** —— 上一位同学关掉的公告，下一位登录时应该重新看到；
2. **拉公告失败要静默吞掉**：公告是附加信息，不能因为它拉不到就让用户进不了网盘。

管理端（仅超管）：`GET/POST /admin/announcements`、`PUT /admin/announcements/{id}`、
`PUT /admin/announcements/{id}/publish`、`PUT /admin/announcements/{id}/recall`、
`DELETE /admin/announcements/{id}`。详见后端接口手册 §8。

> ⚠️ **已发布的公告不能直接改**（返回 `40095`），必须先「撤回」再改。
> 界面上的做法：`status=1` 的行把「编辑」「删除」置灰，只留「撤回」，
> 鼠标悬停给出原因，而不是让用户点了才知道。

### 3.8 人机验证（登录 / 注册 / 上传共用一样板）

后台维护的图片验证码题库会在三个动作前拦一道。**三处用同一个弹窗、同一段逻辑**，
建议做成项目级的一个弹窗组件 + 一个小 store（本项目是
`components/HumanCheckDialog.vue` + `stores/humanCheck.ts`，可直接照搬）。

```ts
// 1) 进页面先问一次（公开接口，不占鉴权）
const human = await get<HumanCheckVO>('/auth/human-check')
// { login, register, upload, passTtlSeconds }

// 2) 动作前：需要就弹窗拿凭证
async function ensure(scope: 'login' | 'register' | 'upload', force = false) {
  if (!force && !human[scope]) return null          // 服务端说不需要
  if (cachedToken && !expired(cachedToken)) return cachedToken   // 复用未过期的
  return await openCaptchaDialog()                  // 用户取消时返回 null
}

// 3) 带着凭证调真正的接口；收到 40105 / 40103 就 force 再弹一次并重试
```

五个必须注意的点：

1. **不要自己写死"要不要验证"**：`/auth/human-check` 给的是**生效值**，
   后台题库为空时全是 `false`（否则所有人都会被挡在登录页且无法完成验证）；
2. **收到 `40105` / `40103` 必须能再弹一次**，不能只信页面加载时那份配置 ——
   管理员可能在你页面停留期间刚上传了第一批题目；
3. **并发要合并**：一次上传会并发申请多个凭证，若每个都弹窗，用户会被弹窗淹没
   （store 里用 in-flight promise + 凭证复用解决）；
4. **凭证的消费规则不一样**：登录 / 上传的凭证**不消费**（5 分钟内可重用，
   所以密码输错重试不必重做验证码；上传另有 10 分钟免验证窗口），
   **注册发码的凭证是一次性的**，每次发送都要重新验证；
5. **用户取消就中止动作**，别把"取消"当成"验证失败"报红字 —— 这不是错误。

> ⚠️ 上传的验证码要**在开始上传之前**弹（本项目在"选完文件、开始建目录"时），
> 不要等每个文件申请凭证时才弹：那时用户已经在盯指纹进度条，突然弹出验证码很割裂。

---

## 4. 页面 → 接口映射

| 页面 | 接口 |
|------|------|
| 登录页 | `GET /auth/register-config`、`GET /auth/human-check`、`POST /auth/login` |
| 注册弹窗 | `GET /auth/captcha` → `POST /auth/captcha/verify` → `POST /auth/email-code` → `POST /auth/register` |
| 强制改密 | `POST /auth/password` |
| 文件列表 | `GET /user/profile`、`GET /files`、`GET /files/tree`、`GET /files/breadcrumb`、`POST /folders`、`PUT /files/rename`、`POST /files/move`、`POST /files/copy`、`DELETE /files`、`GET /files/{id}/download-url` |
| 在线阅览 | `GET /files/{id}/preview-url`（image/video/audio）或 `GET /files/{id}/text`（text/office）；**pdf 不可预览**，看 `previewable` |
| 图片管理 | `GET /images` |
| 上传面板 | `GET /user/quota`、`POST /oss/ticket`、`POST /oss/put-url`、`POST /oss/multipart/*`、`POST /files/instant-upload`、`POST /files/commit` |
| 回收站 | `GET /recycle`、`POST /recycle/restore`、`DELETE /recycle/purge`、`DELETE /recycle/empty` |
| 个人信息 | `GET /user/profile`、`PUT /user/profile`、`POST /user/avatar`、`DELETE /user/avatar`、`GET /user/quota` |
| 人机验证（登录/注册/上传共用） | `GET /auth/human-check`、`GET /auth/captcha`、`POST /auth/captcha/verify` |
| 公告（全站横幅/弹窗） | `GET /announcements/active` |
| 管理后台-名单导入 | `GET /admin/students/import-template`、`POST /admin/students/import` |
| 管理后台-用户管理 | `GET /admin/users`、`PUT /admin/users/{id}/{status,quota,reset-password,role}`、`PUT /admin/users/reset-password-batch`、`DELETE /admin/users/{id}` |
| 管理后台-题库 | `GET /admin/captchas`、`POST /admin/captchas/upload`、`PUT /admin/captchas/{id}`、`DELETE /admin/captchas/{id}` |
| 管理后台-公告（超管） | `GET/POST /admin/announcements`、`PUT /admin/announcements/{id}`、`PUT /admin/announcements/{id}/publish`、`PUT /admin/announcements/{id}/recall`、`DELETE /admin/announcements/{id}` |
| 管理后台-运维（超管） | `/admin/ops/**`（含 `POST /admin/ops/oss-reconcile?dryRun=true` 预览清理） |

---

## 5. 权限与界面显隐

`GET /user/profile` 返回 `role`，顶部导航按它显隐（**前端只做体验控制，真正的鉴权在后端**）：

| role | 含义 | 能进 |
|:----:|------|------|
| `0` | 学生 | 只能进自己的网盘 |
| `2` | 教师（机房管理员） | + 名单导入、查看学生列表、重置学生密码 |
| `1` | 管理员 | + 用户禁用/配额/容量重算、验证码题库 |
| `9` | 超级管理员 | + 任命角色、删除账号、**公告管理**、运维接口 |

---

## 6. 联调与自测

后端提供了端到端验收脚本，**不需要前端就能验证后端是否正常**：

```powershell
pwsh ./scripts/api-smoke.ps1 -Login admin -Password 'YourInitPassword'
```

它会跑：登录 → 个性属性 → 头像上传/读回/更换/清除 → 上传（含 OSS 直传与 commit）→
下载回本地并逐字节比对 MD5 → Range 断点续传 → 回收站。前端联调出问题时，
先跑一遍这个脚本，能立刻区分是"后端坏了"还是"前端调用姿势不对"。

> 注意：冒烟脚本目前**不覆盖公告**（公告需要超管账号且会改数据），
> 且**题库启用后需要人机验证**（登录与申请上传凭证会返回 40105）：
>
> ```powershell
> # 传一个凭证（浏览器过一次验证码，从 /auth/captcha/verify 响应里取，5 分钟内有效）
> pwsh ./scripts/api-smoke.ps1 -Login admin -Password 'xxx' -CaptchaPassToken 5f3c...
> ```
>
> 公告接口请用后端接口手册 §8 的 curl 手工验一遍。

---

## 7. 常见坑清单

| 现象 | 原因 / 解法 |
|------|------------|
| 请求全部 40100 | 请求头字段名必须是 `Authorization`，值是**原始 token**，不加 `Bearer` |
| `<img src>` 图片裂开 | 用了需要鉴权的流式地址。改用 `avatarUrl` / `preview-url` 返回的**签名地址** |
| PUT OSS 报 `SignatureDoesNotMatch` | `Content-Type` 与预签名时不一致 —— 用 `/oss/put-url` 与 `/oss/multipart/part-urls` 响应里返回的 `contentType` |
| 上传 100% 但文件不见了 | 只做了直传、没调 `/files/commit`。commit 成功才算保存 |
| 分片上传报参数错误 | `complete` 的 `parts` 必须按 `partNumber` **升序**，且 `etag` 要去掉双引号 |
| 大文件传到一半失败 | `uploadToken` 有效期 2 小时；超时要重新走 `/oss/ticket`（会换 ObjectKey，已传分片作废） |
| 视频拖不了进度条 | 用了签名地址但 OSS 的 CORS 没暴露 `Range`/`ETag` 头，或用了不支持 Range 的接口。`/files/{id}/preview` 支持 Range |
| 文本乱码 | 用 `/files/{id}/text` 而不是自己 fetch 原始字节 —— 后端会处理 GBK / BOM |
| 中文文件名下载后变 `download.docx` | 说明浏览器没解析 `filename*`；后端已同时给 ASCII 回退名，正常浏览器会用 UTF-8 名 |
| 图片/头像换成新的但界面还是旧的 | 用 `avatarVersion` / 换签名地址做缓存失效 |
| 分页第一页是空的 | `page` 从 **1** 开始，不是 0 |
| 搜索不到文件 | `keyword` 是**前缀匹配**，界面文案改成"输入开头几个字" |
| 回收站清空了容量没变 | 容量是缓存值；等每日对账，或超管调 `POST /admin/ops/reconcile-storage` |
| 内网 http 下 `crypto.randomUUID is not a function` | 非安全上下文没有 Web Crypto。用后端签发的 `uploadKey`，本地 ID 自己用随机字符串生成 |
| 点"在线预览"报 40073 | 该文件 `previewable=false`（pdf 就是如此）。入口显隐请一律看 `previewable` |
| 换头像报 40123 | 24 小时冷却未到。按 `avatarChangeableAt` 置灰按钮并显示剩余时间 |
| 改公告报 40095 | 公告已发布，必须先 `recall` 撤回再改 |
| 公告点了"发布"用户却看不到 | 检查 `expireTime` 是否已过；后台列表的 `effective` 会直接告诉你 |
| 登录/上传突然返回 **40105** | 后台题库被启用了（管理员刚上传题目）。弹一次验证码窗口、拿新凭证重试即可 —— 别只信页面加载时那份 `human-check` 配置 |
| 用户被反复要求做验证码 | ① 每次调用都用 `force=true` 弹窗（应只在收到 40105 时 force）；② 上传免验证窗口被关小了（`app.captcha.upload-pass-minutes`） |
| 验证码弹窗一打开就报"题库为空" | 题库此刻确实没有可用题目（服务端会同时把 `human-check` 变成 false）。此时应**不弹窗**，让用户继续动作 |
| 注册点"重新发送"提示凭证无效 | 注册的凭证是**一次性**的，重发必须重新过一次验证码（见 §3.8） |
| **点「刷新」按钮就报"加载失败：[40000] 参数类型错误: page"**，而首次进页面正常 | 🔴 `@click="reload"` 而 `reload(toPage?: number)` —— Vue 会把**事件对象**当第一个实参传进去，`page` 于是变成 `[object PointerEvent]`，服务端 `Long page` 绑不上。**这类缺陷 esbuild / rollup 与 `tsc --noEmit` 都报不出来**（`.vue` 模板不在 tsc 的检查范围内） | 写 `@click="reload()"`；函数内部也要判类型（`typeof toPage === 'number'`），**不要只写 `if (toPage)`** —— 事件对象是真值。回归守卫：`npm run check:handlers`（已串进 `npm run build`） |
| 同一个数字在两个地方显示不一致（侧栏红点 vs 审核页顶部） | 两处各拉了一次接口，或其中一处没跟着更新 | 把它收敛到一处状态（本项目是 `stores/postReview.ts`），两边读同一份；轮询也只在 store 里起一次 |
| 页面 A 审完一条，页面 B 的列表还挂着已处理的内容 | 缺少"数据变了要通知另一个页面/标签页"的机制 | ① 同一应用内：走共享 store（见上）；② 跨标签页：不做实时推送，靠轮询 + 切回前台补拉（`visibilitychange` / `focus`） |

---

## 8. 后端尚未提供 / 需要约定的事项

| 项 | 说明 |
|----|------|
| 缩略图 | 图片列表返回原图签名地址，前端用 `el-image` 的 `lazy` + 固定网格自行缩放；后端未生成缩略图 |
| 图片宽高 | 列表不返回宽高（避免为每张图额外读 OSS）；前端可用图片 `onload` 后取 `naturalWidth/Height` 做瀑布流 |
| Word/Excel 版面 | `docx` / `xlsx` 由**服务端渲染结构化 HTML**（`/files/{id}/text` 的 `html` 字段）并返回，**不是像素级还原**：字体、行距、分页、浮动图片都不还原。要真正的所见即所得需引入 OnlyOffice/LibreOffice，不在本期范围 |
| 旧版 `.ppt` / `.xls` | 二进制格式没有可靠的无依赖解析方案，`viewType=none`，只能下载（`.doc` / `.docx` / `.pptx` / `.xlsx` 已支持，见上） |
| `.doc` 里的图片 | 内嵌图片接口只覆盖 `docx`（`word/media/`）与 `pptx`（`ppt/media/`）；`.doc` 是 OLE2 二进制，图片取不出来 |
| 打包下载 | 未实现；批量下载请逐个调 `download-url` |
| 预置演示数据 | 无。测试账号请用名单导入接口建，或直接改 `app.register.enabled` 后自助注册 |
| 公告的"已读" | 后端不记录谁读过哪条（不做 per-user 已读表），已读状态由前端自行保存 |
